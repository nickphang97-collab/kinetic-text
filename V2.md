# V2 deferrals

- M3 — finish the render pipeline: audio probe/mux with video-authoritative `apad`/trim, temp cleanup on success/error/signal, disk projection, `--seed`, both-preset/audio/error-path acceptance, and timeout receipts.
- M4 — after M3, build the tolerant golden harness from M2/M3 renders: named frames, five metrics, band SSIM Y ≥ 0.985, committed goldens, explicit update mode, and all four mutation discriminators.
- M5 — run both black-box acceptance scenarios, the ten-script success metric, all three theme renders, final frame set, leak/cleanup audit, full README expansion, and operator visual review.
